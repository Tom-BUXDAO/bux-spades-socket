/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'arweave.net',
      'nftstorage.link'
    ],
  },
  // Add any other Next.js config options here
};

module.exports = nextConfig; 
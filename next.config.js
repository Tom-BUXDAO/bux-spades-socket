/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'cdn.discordapp.com'
    ],
  },
  // Add any other Next.js config options here
};

module.exports = nextConfig; 